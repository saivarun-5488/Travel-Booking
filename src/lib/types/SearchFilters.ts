export interface SearchFilters {
    destination?: string;
    minPrice?: number;
    maxPrice?: number;
    duration?: string;
    travelers?: number;
    startDate?: string;
    endDate?: string;
  }